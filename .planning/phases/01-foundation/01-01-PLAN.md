---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - docker-compose.yml
  - .env.example
  - packages/shared-types/package.json
  - packages/shared-types/tsconfig.json
  - packages/shared-types/vitest.config.ts
  - packages/shared-types/src/index.ts
  - packages/shared-types/src/numbers.ts
  - packages/shared-types/src/numbers.test.ts
  - packages/shared-types/src/events.ts
  - packages/server/package.json
  - packages/server/tsconfig.json
  - packages/server/vitest.config.ts
  - packages/client/package.json
  - packages/client/tsconfig.json
  - packages/client/vitest.config.ts
  - packages/client/vite.config.ts
  - packages/client/index.html
  - packages/client/src/main.tsx
  - packages/client/src/App.tsx
autonomous: true
requirements:
  - UI-02

must_haves:
  truths:
    - "formatNumber(1000) returns '1.0K'"
    - "formatNumber(1e6) returns '1.0M'"
    - "formatNumber(1e9) returns '1.0B'"
    - "formatNumber(Infinity) returns '0'"
    - "formatNumber(NaN) returns '0'"
    - "pnpm install succeeds across all three workspace packages"
    - "Docker Compose starts Redis and PostgreSQL"
    - "vitest runs in shared-types and server packages"
  artifacts:
    - path: "packages/shared-types/src/numbers.ts"
      provides: "formatNumber utility and Decimal re-export"
      exports: ["formatNumber", "Decimal"]
    - path: "packages/shared-types/src/numbers.test.ts"
      provides: "Unit tests for formatNumber covering K/M/B/T/NaN/Infinity"
    - path: "docker-compose.yml"
      provides: "Redis 8 + PostgreSQL 17 dev services"
      contains: "postgres"
    - path: "pnpm-workspace.yaml"
      provides: "Monorepo workspace definition"
      contains: "packages/*"
  key_links:
    - from: "packages/shared-types/src/index.ts"
      to: "packages/shared-types/src/numbers.ts"
      via: "re-export"
      pattern: "export.*from.*numbers"
---

<objective>
Scaffold the pnpm monorepo with three packages (shared-types, server, client), create Docker Compose for Redis + PostgreSQL, and implement the formatNumber utility with full test coverage.

Purpose: Everything in Phase 1 depends on the monorepo structure, dev infrastructure, and the shared-types package. This must land first.
Output: Working monorepo with `pnpm install` succeeding, Docker services startable, formatNumber tested and passing.
</objective>

<execution_context>
@C:/Users/walbr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/walbr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold pnpm monorepo with three packages and Docker Compose</name>
  <files>
    pnpm-workspace.yaml,
    package.json,
    docker-compose.yml,
    .env.example,
    .gitignore,
    packages/shared-types/package.json,
    packages/shared-types/tsconfig.json,
    packages/server/package.json,
    packages/server/tsconfig.json,
    packages/client/package.json,
    packages/client/tsconfig.json,
    packages/client/vite.config.ts,
    packages/client/index.html,
    packages/client/src/main.tsx,
    packages/client/src/App.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
Create the monorepo structure exactly as specified in the research Architecture Patterns section.

1. Root `pnpm-workspace.yaml`:
```yaml
packages:
  - 'packages/*'
```

2. Root `package.json`:
```json
{
  "name": "killing-blow",
  "private": true,
  "scripts": {
    "dev:server": "pnpm --filter server dev",
    "dev:client": "pnpm --filter client dev",
    "test": "pnpm -r test --run",
    "test:server": "pnpm --filter server test --run",
    "test:shared": "pnpm --filter shared-types test --run"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
```

3. `docker-compose.yml` at repo root:
```yaml
version: '3.9'
services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: killingblow
      POSTGRES_PASSWORD: killingblow
      POSTGRES_DB: killingblow_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
  redis:
    image: redis:8-alpine
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
volumes:
  postgres_data:
  redis_data:
```

4. `.env.example`:
```
DATABASE_URL=postgresql://killingblow:killingblow@localhost:5432/killingblow_dev
REDIS_URL=redis://localhost:6379
JWT_SECRET=change-me-in-production
SERVER_URL=http://localhost:3000
CLIENT_URL=http://localhost:5173
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
```

5. `.gitignore` — add node_modules, dist, .env (not .env.example), .prisma, coverage entries.

6. `packages/shared-types/package.json`:
```json
{
  "name": "@killing-blow/shared-types",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest"
  },
  "dependencies": {
    "break_eternity.js": "^2.1.3"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^4.1.0"
  }
}
```

7. `packages/shared-types/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "declaration": true,
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src"]
}
```

8. `packages/server/package.json`:
```json
{
  "name": "@killing-blow/server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "test": "vitest",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@killing-blow/shared-types": "workspace:*",
    "fastify": "^5.8.2",
    "@fastify/jwt": "^10.0.0",
    "@fastify/oauth2": "^8.2.0",
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^11.2.0",
    "@fastify/rate-limit": "^10.3.0",
    "@fastify/websocket": "^11.2.0",
    "socket.io": "^4.8.3",
    "prisma": "^7.5.0",
    "@prisma/client": "^7.5.0",
    "redis": "^5.11.0",
    "argon2": "^0.44.0",
    "zod": "^4.3.6",
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "tsx": "^4.19.0",
    "vitest": "^4.1.0",
    "@types/node": "^22.0.0"
  }
}
```

9. `packages/server/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "outDir": "dist",
    "rootDir": "src",
    "resolveJsonModule": true
  },
  "include": ["src"]
}
```

10. `packages/client/package.json`:
```json
{
  "name": "@killing-blow/client",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest"
  },
  "dependencies": {
    "@killing-blow/shared-types": "workspace:*",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "react-router-dom": "^7.13.1",
    "zustand": "^5.0.12",
    "socket.io-client": "^4.8.3"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.0",
    "typescript": "^5.8.0",
    "vite": "^8.0.0",
    "vitest": "^4.1.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
```

11. `packages/client/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "jsx": "react-jsx",
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src"]
}
```

12. `packages/client/vite.config.ts`:
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      '/api': 'http://localhost:3000',
      '/auth': 'http://localhost:3000',
      '/socket.io': {
        target: 'http://localhost:3000',
        ws: true
      }
    }
  }
})
```

13. `packages/client/index.html`:
```html
<!DOCTYPE html>
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Killing Blow</title>
  </head>
  <body class="bg-zinc-950 text-zinc-50">
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

14. `packages/client/src/main.tsx` — minimal React 19 entry point rendering `<App />` into `#root`.

15. `packages/client/src/App.tsx` — minimal component returning `<div>Killing Blow</div>` placeholder.

After creating all files, run `pnpm install` from the repo root.
  </action>
  <verify>
    <automated>cd C:/Users/walbr/Downloads/gsd && pnpm install && echo "INSTALL_OK"</automated>
  </verify>
  <acceptance_criteria>
    - pnpm-workspace.yaml contains "packages/*"
    - docker-compose.yml contains "postgres:17" and "redis:8-alpine"
    - .env.example contains "DATABASE_URL=" and "JWT_SECRET=" and "GOOGLE_CLIENT_ID=" and "DISCORD_CLIENT_ID="
    - packages/shared-types/package.json contains "break_eternity.js"
    - packages/server/package.json contains "fastify" and "@fastify/jwt" and "argon2" and "prisma" and "socket.io"
    - packages/client/package.json contains "react" and "zustand" and "react-router-dom"
    - packages/client/index.html contains 'class="dark"'
    - pnpm install exits with code 0
  </acceptance_criteria>
  <done>All three workspace packages resolve, pnpm install succeeds, Docker Compose file ready to start Redis + PostgreSQL</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement formatNumber utility with full test coverage</name>
  <files>
    packages/shared-types/src/numbers.ts,
    packages/shared-types/src/numbers.test.ts,
    packages/shared-types/src/events.ts,
    packages/shared-types/src/index.ts,
    packages/shared-types/vitest.config.ts,
    packages/server/vitest.config.ts,
    packages/client/vitest.config.ts
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
    packages/shared-types/package.json
  </read_first>
  <behavior>
    - formatNumber(0) returns "0.0"
    - formatNumber(999) returns "999.0"
    - formatNumber(1000) returns "1.0K"
    - formatNumber(1500) returns "1.5K"
    - formatNumber(1000000) returns "1.0M"
    - formatNumber(1e9) returns "1.0B"
    - formatNumber(1e12) returns "1.0T"
    - formatNumber(1e15) returns "1.0Qa"
    - formatNumber(Infinity) returns "0"
    - formatNumber(NaN) returns "0"
    - formatNumber(-500) returns "-500.0"
    - formatNumber(new Decimal("1e308")) returns scientific notation string
    - Decimal is re-exported from numbers.ts
  </behavior>
  <action>
1. Create `packages/shared-types/vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config'
export default defineConfig({
  test: {
    environment: 'node',
    globals: true
  }
})
```

2. Create `packages/server/vitest.config.ts` and `packages/client/vitest.config.ts` with identical content (environment: 'node' for server, environment: 'node' for client — jsdom will be added when UI tests are needed).

3. Create `packages/shared-types/src/numbers.test.ts` with test cases matching the behavior list above. Import `formatNumber` and `Decimal` from `./numbers.js`. Use `describe('formatNumber', () => { ... })` with `it()` blocks for each case.

4. Run tests — they MUST fail (RED phase).

5. Create `packages/shared-types/src/numbers.ts` exactly as specified in the research Pattern 7:
```typescript
import Decimal from 'break_eternity.js'
export { Decimal }

const SUFFIXES = ['', 'K', 'M', 'B', 'T', 'Qa', 'Qi', 'Sx', 'Sp', 'Oc', 'No', 'Dc']

export function formatNumber(value: Decimal | number | string): string {
  const d = value instanceof Decimal ? value : new Decimal(value)
  if (!d.isFinite() || d.isNan()) return '0'
  const absVal = d.abs()
  if (absVal.lt(1000)) return d.toFixed(1)
  const exp = Math.floor(absVal.log10().toNumber())
  const tier = Math.floor(exp / 3)
  if (tier >= SUFFIXES.length) {
    return d.toExponential(2)
  }
  const scale = new Decimal(10).pow(tier * 3)
  const scaled = d.div(scale)
  return `${scaled.toFixed(1)}${SUFFIXES[tier]}`
}
```

6. Run tests — they MUST pass (GREEN phase).

7. Create `packages/shared-types/src/events.ts` with placeholder Socket.IO event type contracts:
```typescript
export interface ServerToClientEvents {
  // Phase 2 will add boss:hp_update, boss:death, kb:winner
}

export interface ClientToServerEvents {
  // Phase 2 will add attack:intent
}
```

8. Create `packages/shared-types/src/index.ts`:
```typescript
export { Decimal, formatNumber } from './numbers.js'
export type { ServerToClientEvents, ClientToServerEvents } from './events.js'
```
  </action>
  <verify>
    <automated>cd C:/Users/walbr/Downloads/gsd && pnpm --filter shared-types test --run</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared-types/src/numbers.ts contains "export function formatNumber"
    - packages/shared-types/src/numbers.ts contains "export { Decimal }"
    - packages/shared-types/src/numbers.ts contains "SUFFIXES" with values "K", "M", "B", "T", "Qa"
    - packages/shared-types/src/numbers.test.ts contains "1.0K"
    - packages/shared-types/src/numbers.test.ts contains "1.0M"
    - packages/shared-types/src/numbers.test.ts contains "1.0B"
    - packages/shared-types/src/index.ts contains "export" and "formatNumber" and "Decimal"
    - packages/shared-types/vitest.config.ts exists
    - packages/server/vitest.config.ts exists
    - packages/client/vitest.config.ts exists
    - pnpm --filter shared-types test --run exits with code 0
  </acceptance_criteria>
  <done>formatNumber correctly formats numbers with K/M/B/T suffixes, handles Infinity and NaN as "0", all tests pass, vitest configs exist for all three packages</done>
</task>

</tasks>

<verification>
- `pnpm install` succeeds at repo root
- `pnpm --filter shared-types test --run` passes all formatNumber tests
- docker-compose.yml is valid (docker compose config)
- All three packages have vitest.config.ts
- .env.example has all required variables
</verification>

<success_criteria>
- Monorepo structure matches research architecture exactly
- formatNumber handles K/M/B/T/Qa suffixes, Infinity, NaN
- All tests pass
- Docker Compose ready for PostgreSQL 17 + Redis 8
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
