---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/client/src/index.css
  - packages/client/src/main.tsx
  - packages/client/src/App.tsx
  - packages/client/components.json
  - packages/client/tailwind.config.ts
  - packages/client/src/pages/Register.tsx
  - packages/client/src/pages/Login.tsx
  - packages/client/src/stores/sessionStore.ts
  - packages/client/src/lib/api.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "Register page renders at /register with username input, password input, and 'Create account' submit button"
    - "Login page renders at /login with username input, password input, and 'Sign in' submit button"
    - "Register form submits POST /auth/register and stores session on success"
    - "Login form submits POST /auth/login and stores session on success"
    - "OAuth buttons render on both pages: 'Continue with Google' and 'Continue with Discord'"
    - "Navigation link between Register and Login pages works"
    - "Session store holds userId, username, isAuthenticated"
    - "All interactive elements have hover, focus, and disabled/loading states"
  artifacts:
    - path: "packages/client/src/pages/Register.tsx"
      provides: "Register page with form and OAuth buttons"
      contains: "Create account"
    - path: "packages/client/src/pages/Login.tsx"
      provides: "Login page with form and OAuth buttons"
      contains: "Sign in"
    - path: "packages/client/src/stores/sessionStore.ts"
      provides: "Zustand session store"
      exports: ["useSessionStore"]
    - path: "packages/client/components.json"
      provides: "shadcn configuration"
  key_links:
    - from: "packages/client/src/pages/Register.tsx"
      to: "packages/client/src/stores/sessionStore.ts"
      via: "useSessionStore for login/setSession"
      pattern: "useSessionStore"
    - from: "packages/client/src/pages/Register.tsx"
      to: "/auth/register"
      via: "fetch POST"
      pattern: "fetch.*auth/register"
    - from: "packages/client/src/App.tsx"
      to: "packages/client/src/pages/Register.tsx"
      via: "react-router-dom Route"
      pattern: "Route.*register"
---

<objective>
Set up the React frontend with shadcn/ui (zinc dark theme), Tailwind CSS, routing, session store, and implement the Register and Login pages matching the UI-SPEC exactly.

Purpose: The frontend auth pages are the user-facing entry point. Register and Login forms connect to the backend auth API. The session store manages JWT-based auth state.
Output: Two functional auth pages with shadcn components, proper form validation, loading states, and error handling.
</objective>

<execution_context>
@C:/Users/walbr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/walbr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui with zinc dark theme, install components, set up Tailwind and routing</name>
  <files>
    packages/client/components.json,
    packages/client/tailwind.config.ts,
    packages/client/src/index.css,
    packages/client/src/main.tsx,
    packages/client/src/App.tsx,
    packages/client/src/lib/utils.ts,
    packages/client/index.html
  </files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md
    packages/client/package.json
    packages/client/index.html
    packages/client/src/main.tsx
  </read_first>
  <action>
1. Install Tailwind CSS and its Vite plugin:
```bash
cd packages/client
pnpm add -D tailwindcss @tailwindcss/vite
```

2. Run shadcn init:
```bash
npx shadcn@latest init --defaults
```
Select: style = Default, base color = Zinc, CSS variables = yes.

3. After init, verify `packages/client/components.json` exists. Update `tailwind.config.ts` to set `darkMode: "class"`.

4. Confirm `packages/client/index.html` has `class="dark"` on the `<html>` element (already set in Plan 01).

5. Add the accent color override in `packages/client/src/index.css` inside the `.dark` block:
```css
.dark {
  --primary: 0 72% 51%;           /* hsl equivalent of #DC2626 */
  --primary-foreground: 0 0% 98%;
}
```

6. Install required shadcn components:
```bash
npx shadcn@latest add button input label card separator
```

7. Update `packages/client/src/main.tsx` to include BrowserRouter:
```typescript
import React from 'react'
import ReactDOM from 'react-dom/client'
import { BrowserRouter } from 'react-router-dom'
import App from './App.js'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>
)
```

8. Update `packages/client/src/App.tsx` with route definitions:
```typescript
import { Routes, Route, Navigate } from 'react-router-dom'
import Register from './pages/Register.js'
import Login from './pages/Login.js'

export default function App() {
  return (
    <div className="min-h-screen bg-zinc-950 text-zinc-50 font-sans">
      <Routes>
        <Route path="/register" element={<Register />} />
        <Route path="/login" element={<Login />} />
        <Route path="/" element={<Navigate to="/login" replace />} />
      </Routes>
    </div>
  )
}
```

9. Update `packages/client/vite.config.ts` to include the Tailwind plugin:
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  server: {
    port: 5173,
    proxy: {
      '/api': 'http://localhost:3000',
      '/auth': 'http://localhost:3000',
      '/socket.io': {
        target: 'http://localhost:3000',
        ws: true
      }
    }
  }
})
```
  </action>
  <verify>
    <automated>cd C:/Users/walbr/Downloads/gsd/packages/client && ls components.json && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/client/components.json exists and contains "shadcn"
    - packages/client/index.html contains 'class="dark"'
    - packages/client/src/index.css contains "--primary: 0 72% 51%"
    - packages/client/src/App.tsx contains "Route" and "/register" and "/login"
    - packages/client/src/main.tsx contains "BrowserRouter"
    - shadcn components exist: packages/client/src/components/ui/button.tsx, input.tsx, label.tsx, card.tsx, separator.tsx
  </acceptance_criteria>
  <done>shadcn/ui initialized with zinc dark theme, accent overridden to red-600, five components installed, routing configured for /register and /login</done>
</task>

<task type="auto">
  <name>Task 2: Session store, API helper, Register page, and Login page</name>
  <files>
    packages/client/src/stores/sessionStore.ts,
    packages/client/src/lib/api.ts,
    packages/client/src/pages/Register.tsx,
    packages/client/src/pages/Login.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md
    packages/client/src/App.tsx
    packages/client/src/components/ui/button.tsx
    packages/client/src/components/ui/input.tsx
    packages/client/src/components/ui/card.tsx
  </read_first>
  <action>
1. Create `packages/client/src/stores/sessionStore.ts`:
```typescript
import { create } from 'zustand'

interface Session {
  userId: string | null
  username: string | null
  isAuthenticated: boolean
  isLoading: boolean
}

interface SessionActions {
  setSession: (userId: string, username: string) => void
  clearSession: () => void
  setLoading: (loading: boolean) => void
  checkSession: () => Promise<void>
}

export const useSessionStore = create<Session & SessionActions>((set) => ({
  userId: null,
  username: null,
  isAuthenticated: false,
  isLoading: true,

  setSession: (userId, username) =>
    set({ userId, username, isAuthenticated: true, isLoading: false }),

  clearSession: () =>
    set({ userId: null, username: null, isAuthenticated: false, isLoading: false }),

  setLoading: (loading) => set({ isLoading: loading }),

  checkSession: async () => {
    try {
      const res = await fetch('/auth/me', { credentials: 'include' })
      if (res.ok) {
        const user = await res.json()
        set({ userId: user.id, username: user.username, isAuthenticated: true, isLoading: false })
      } else {
        set({ isAuthenticated: false, isLoading: false })
      }
    } catch {
      set({ isAuthenticated: false, isLoading: false })
    }
  }
}))
```

2. Create `packages/client/src/lib/api.ts`:
```typescript
export async function apiPost(path: string, body: Record<string, unknown>): Promise<{ ok: boolean; status: number; data: any }> {
  const res = await fetch(path, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    credentials: 'include',
    body: JSON.stringify(body)
  })
  const data = await res.json().catch(() => ({}))
  return { ok: res.ok, status: res.status, data }
}
```

3. Create `packages/client/src/pages/Register.tsx` matching UI-SPEC exactly:

Layout: centered single-column, auth card at vertical center (offset 48px from top on tall screens).

Elements inside Card (top to bottom):
- Heading: "Create account" — use `<h1 className="text-xl font-semibold leading-[1.2] text-zinc-50">Create account</h1>`
- Username Label + Input (full-width, `id="username"`, `htmlFor="username"`)
- Password Label + Input (full-width, `type="password"`, `id="password"`, `htmlFor="password"`)
- Submit Button: full-width, variant="default" (uses accent via CSS variable override), label "Create account", shows Loader2 spinner during submission, disabled while loading
- Separator
- "Or continue with" text — 14px, zinc-400
- "Continue with Google" Button: variant="outline", full-width, min-h-[44px], links to `/auth/google`
- "Continue with Discord" Button: variant="outline", full-width, min-h-[44px], links to `/auth/discord`
- Link to Login: "Already have an account? " + `<Link to="/login" className="underline">Sign in</Link>`

Form behavior:
- Client-side validation: username not empty ("This field is required."), password >= 8 chars ("Password must be at least 8 characters.")
- On submit: call `apiPost('/auth/register', { username, password })`
- On 200: call `useSessionStore.getState().setSession(data.id, data.username)` and navigate to `/profile`
- On 409: show "That username is already taken. Try a different one." below username input
- On 5xx: show "Something went wrong. Please try again."
- Error text: 14px, text-red-500, associated via `aria-describedby`

All interactive states per UI-SPEC:
- Button hover: `hover:bg-primary/90`
- Button focus: `focus:ring-2 focus:ring-primary focus:ring-offset-2`
- Button disabled/loading: `disabled:opacity-50 disabled:cursor-not-allowed`
- Input hover: `hover:bg-zinc-800 hover:border-zinc-600`
- Input focus: `focus:ring-2 focus:ring-primary focus:ring-offset-2`

4. Create `packages/client/src/pages/Login.tsx` — identical structure to Register but:
- Heading: "Sign in"
- Submit button label: "Sign in"
- Calls `apiPost('/auth/login', { username, password })`
- On 401: show "Incorrect username or password."
- Footer link: "No account? " + `<Link to="/register" className="underline">Create one</Link>`

5. Add `/profile` route placeholder in App.tsx — just renders "Profile coming in Plan 04". This prevents navigation errors after successful register/login.
  </action>
  <verify>
    <automated>cd C:/Users/walbr/Downloads/gsd/packages/client && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - packages/client/src/stores/sessionStore.ts contains "useSessionStore" and "setSession" and "checkSession" and "clearSession"
    - packages/client/src/lib/api.ts contains "apiPost" and "credentials: 'include'"
    - packages/client/src/pages/Register.tsx contains "Create account" (heading and button text)
    - packages/client/src/pages/Register.tsx contains "Continue with Google" and "Continue with Discord"
    - packages/client/src/pages/Register.tsx contains "Already have an account?" and "/login"
    - packages/client/src/pages/Register.tsx contains "That username is already taken"
    - packages/client/src/pages/Register.tsx contains "aria-describedby"
    - packages/client/src/pages/Register.tsx contains "Loader2"
    - packages/client/src/pages/Login.tsx contains "Sign in" (heading and button text)
    - packages/client/src/pages/Login.tsx contains "Incorrect username or password"
    - packages/client/src/pages/Login.tsx contains "No account?" and "/register"
    - packages/client/src/pages/Login.tsx contains "min-h-[44px]" or "min-h-11" (44px OAuth button height)
    - vite build exits with code 0
  </acceptance_criteria>
  <done>Register and Login pages render with exact UI-SPEC copy, shadcn components, proper validation, loading states, OAuth button links, and session store wired</done>
</task>

</tasks>

<verification>
- `vite build` succeeds without TypeScript or React errors
- Register page has all elements from UI-SPEC Screen Contract
- Login page has all elements from UI-SPEC Screen Contract
- Session store manages auth state
- OAuth buttons link to /auth/google and /auth/discord
- All error messages match Copywriting Contract exactly
</verification>

<success_criteria>
- shadcn/ui zinc dark theme active with red-600 accent
- Register and Login pages match UI-SPEC pixel-for-pixel
- All copy matches Copywriting Contract (no deviations)
- All interaction states implemented (hover, focus, disabled, loading)
- Accessibility: labels linked via htmlFor, aria-describedby on errors, 44px OAuth buttons
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
