---
phase: 02-core-boss-loop
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified: []
autonomous: false
requirements: [BOSS-01, BOSS-02, BOSS-03, KB-01, KB-04, UI-01]

must_haves:
  truths:
    - "Two browser tabs show the same boss HP draining in real time (BOSS-01)"
    - "Floating damage numbers appear only in the attacking tab (BOSS-02)"
    - "Boss death animation plays and next boss spawns without page reload (BOSS-03)"
    - "Player sidebar lists all players with damage contribution (UI-01)"
    - "Kill winner is determined server-side — no client damage values (KB-01)"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 2 boss loop works end-to-end in a real browser with Docker services running.

Purpose: All automated tests pass, but the visual experience (animations, real-time sync across tabs, defeat overlay) requires human eyes. This checkpoint validates the full user journey before marking Phase 2 complete.

Output: Human confirmation that all Phase 2 success criteria are met in a live browser session.
</objective>

<execution_context>
@C:/Users/walbr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/walbr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-boss-loop/02-CONTEXT.md
@.planning/phases/02-core-boss-loop/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and start services</name>
  <files></files>
  <read_first>
    packages/server/vitest.config.ts,
    docker-compose.yml
  </read_first>
  <action>
**1. Run the full test suite:**
```bash
pnpm -r test --run
```
All tests must pass (server bossState tests, gateway tests, shared-types build).

**2. Ensure Docker services are running:**
```bash
docker compose up -d
```

**3. Run Prisma migration if needed:**
```bash
cd packages/server && npx prisma migrate dev
```

**4. Start the server:**
```bash
pnpm --filter server dev
```

**5. Start the client:**
```bash
pnpm --filter client dev
```

Confirm both processes start without errors.
  </action>
  <verify>
    <automated>pnpm -r test --run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm -r test --run` exits 0 with 0 failed tests
    - docker compose ps shows redis and postgres containers running
    - Server process starts without crash on port 3000
    - Client dev server starts without crash on port 5174
  </acceptance_criteria>
  <done>Full test suite green, Docker services running, server and client processes started.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete boss loop in browser</name>
  <files></files>
  <action>
Present the verification steps to the user and wait for their confirmation. The user will test the complete Phase 2 boss loop in a live browser session with two accounts across two tabs.
  </action>
  <verify>User confirms all verification steps pass by typing "approved"</verify>
  <done>Human has verified all Phase 2 success criteria in a live browser session.</done>
  <what-built>
    Complete Phase 2 boss loop: real-time shared HP bar, floating damage numbers, boss death/respawn cycle, active player sidebar, and server-authoritative kill detection.
  </what-built>
  <how-to-verify>
    1. Open http://localhost:5174 in browser Tab A — log in (register if needed)
    2. You should be redirected to /game and see "Boss #1" with HP bar showing 1,000 / 1,000
    3. Click "Attack Boss" in Tab A — verify:
       - HP bar decreases (1,000 -> 975)
       - A yellow floating number "-25" appears and fades upward
       - Player sidebar shows your username with "25 dmg"
    4. Open http://localhost:5174 in browser Tab B (different browser or incognito for different user)
       - Register a second account and navigate to /game
       - Verify Tab B shows the SAME HP value as Tab A (BOSS-01: shared HP)
    5. Click "Attack Boss" in Tab B — verify:
       - BOTH tabs show HP decrease
       - Only Tab B shows the floating damage number (BOSS-02: attacker only)
       - Player sidebar in both tabs shows both players sorted by damage
    6. Keep clicking in either tab until boss HP reaches 0 — verify:
       - Boss sprite fades out with scale animation (~400ms) — BOSS-03
       - Defeat overlay shows "Defeated by [username] — new boss spawning..."
       - A new boss ("Boss #2") appears without page reload — BOSS-03
       - HP bar resets to 1,000 / 1,000
       - Player sidebar clears and shows "Waiting for players..."
    7. Attack the new boss to confirm the cycle repeats
  </how-to-verify>
  <resume-signal>Type "approved" if all verifications pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All Phase 2 success criteria from ROADMAP.md verified in browser:
1. Two tabs show same boss HP bar draining in real time (BOSS-01)
2. Floating damage numbers appear for own hits only (BOSS-02)
3. Boss death animation plays and next boss spawns without reload (BOSS-03)
4. Active player sidebar shows players with damage contribution (UI-01)
5. Kill winner determined server-side (KB-01) — verified by defeat overlay showing correct username
</verification>

<success_criteria>
Human confirms all 7 verification steps pass in a live browser session with two accounts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-boss-loop/02-04-SUMMARY.md`
</output>
