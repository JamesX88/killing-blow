---
phase: 4
slug: competition-and-social
status: draft
shadcn_initialized: true
preset: base-nova / neutral / oklch
created: 2026-03-19
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn v4 | components.json |
| Style | base-nova | components.json |
| Preset | `base-nova`, baseColor: neutral, cssVariables: true | components.json |
| Component library | @base-ui/react (shadcn v4 primitives) | STATE.md decision [Phase 01-foundation] |
| Icon library | lucide-react | components.json → iconLibrary: lucide |
| Font | Inter, system-ui, sans-serif | index.css @theme --font-sans |
| Animation | motion v12 (`motion/react`) | RESEARCH.md Standard Stack |
| Third-party registries | none | components.json registries: {} |

---

## Spacing Scale

Declared values (multiples of 4 only — 8-point base scale):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge inner padding, inline spacing |
| sm | 8px | Compact element spacing, list item gaps |
| md | 16px | Default element spacing, card padding |
| lg | 24px | Section padding, overlay content gaps |
| xl | 32px | Layout gaps between panels |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions:
- Interactive buttons: minimum height 44px (touch target floor — established in UpgradePanel and OfflineRewardToast)
- Overlay backdrop: `fixed inset-0` (full viewport — established pattern in OfflineRewardToast)

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 14px | 400 (regular) | 1.5 | Descriptions, labels, sidebar player names, lore text, badges, mono stat readouts |
| Label | 16px | 600 (semibold) | 1.5 | Stat names, section subheadings, gold balance label |
| Heading | 20px | 600 (semibold) | 1.2 | Panel headings (e.g. "Upgrades"), boss name on lore card |
| Display | 28px | 600 (semibold) | 1.1 | Numeric values (HP, damage), KB Currency balance |

Declared scale: 14px, 16px, 20px, 28px — exactly 4 sizes.

Mono usage: numeric stat readouts (e.g. "125 dmg/hit", leaderboard rank column) use 14px, weight 400, line-height 1.5, font-mono. This is the Body role with font-mono applied — it does not constitute an additional size.

Sources: UpgradePanel.tsx, BossHpBar.tsx, OfflineRewardToast.tsx (direct codebase inspection)

---

## Color

All values are CSS custom properties from index.css. oklch color space.

| Role | CSS Variable | Approximate Hex | Usage |
|------|-------------|-----------------|-------|
| Dominant (60%) | `--background` | oklch(0.141 0.005 285.823) ≈ #18181b | Page background, full-screen overlay backdrops |
| Secondary (30%) | `--card` / `--secondary` | oklch(0.178 0.006 285.886) ≈ #1f1f23 | Cards, sidebars, panels, nav elements |
| Accent (10%) | `--primary` | oklch(0.497 0.209 27.157) = red-600 #dc2626 | Reserved — see accent list below |
| Destructive | `--destructive` | oklch(0.704 0.191 22.216) ≈ #ef4444 | Error text, destructive action buttons only |
| Muted text | `--muted-foreground` | oklch(0.705 0.015 286.067) ≈ #a1a1aa | Secondary copy, labels, subtext |
| Foreground | `--foreground` | oklch(0.985 0 0) ≈ #fafafa | Primary text on dark backgrounds |
| Yellow (special) | hardcoded `text-yellow-400` | #facc15 | Gold balance display only — not a theme token |

Accent (`--primary` red-600) reserved for:
1. Primary CTA buttons (shadcn `<Button variant="default">`)
2. Boss HP bar fill (the bar that drains as the boss takes damage)
3. The `--ring` focus ring on interactive elements
4. Active DPS bonus badge background (`bg-primary/20 text-primary`)
5. Killing blow announcement winner name highlight (new — Phase 4)

Accent is NOT used for: decorative borders, hover states on cards, leaderboard row backgrounds, title badge backgrounds.

Source: index.css, BossHpBar.tsx, UpgradePanel.tsx

---

## Component Inventory

New components required for Phase 4 (follow established patterns):

| Component | Pattern | Motion | Overlay |
|-----------|---------|--------|---------|
| `KillingBlowAnnouncement` | OfflineRewardToast overlay pattern — `fixed inset-0 z-50`, Zustand-driven, `AnimatePresence` | `motion.div` scale + fade in/out (`initial={{ opacity: 0, scale: 0.8 }}`) | Yes — full-screen with `bg-black/70` backdrop |
| `PostFightScreen` | Card overlay (narrower than KBA, top-right or centered), auto-dismisses on `boss:spawn` | Slide-in from bottom or fade-in | Yes — partial overlay or inline panel |
| `Leaderboard` | Scrollable table in a Card, paginated, fetches on mount only | None required | No |
| `TitleShop` | Card with item list, similar to UpgradePanel layout | None required | No — in-page panel or modal |
| Boss lore text | Inline below boss name in BossSprite/boss card area | None required | No — inline |
| Player title badge | Inline span next to username in PlayerSidebar and Leaderboard rows | None required | No — inline |

---

## Interaction Contracts

### Killing Blow Announcement (KB-02)

- **Trigger:** `boss:death` socket event received by client
- **Dismiss:** `boss:spawn` socket event (auto-dismiss, no manual close)
- **Duration:** approximately 3 seconds (until next boss spawns)
- **Layout:** full-screen centered overlay, `z-50`, `bg-black/70` backdrop
- **Enter animation:** `opacity: 0 → 1`, `scale: 0.8 → 1`, spring or 200ms ease-out
- **Exit animation:** `opacity: 1 → 0`, `scale: 1 → 0.8`, 150ms ease-in
- **Winner name color:** `--primary` (red-600) — the one accent use case for this component
- **Title display:** render winner's title (if any) as a muted badge above/below the name at 14px muted-foreground
- **Kill count:** display updated kill count as "Kill #N" in display (28px semibold)
- **Accessibility:** `role="dialog"`, `aria-modal="true"`, `aria-labelledby` pointing to winner heading

### Post-Fight Screen (KB-05)

- **Trigger:** same `boss:death` event as KBA (can be part of same overlay or separate)
- **Dismiss:** `boss:spawn` (auto), same lifecycle as KBA
- **Layout:** top-5 contributors list — username, damage dealt (formatted), optional title badge
- **Implementation choice:** embed as a section within KillingBlowAnnouncement below winner display (single overlay, not two stacked overlays)
- **Row format:** rank number (14px mono), username (14px regular), damage (14px semibold), title badge (14px muted-foreground)

### Leaderboard (KB-03)

- **Navigation:** separate page or in-page tab — not an overlay
- **Fetch strategy:** fetch on mount only, no real-time updates (explicit from RESEARCH.md)
- **Pagination:** page/limit — show 20 rows per page
- **Rank column:** derived from `(page - 1) * limit + rowIndex + 1` — do NOT use stored `kbRank` column
- **Highlight current user:** row background `bg-primary/10` for the authenticated user's row
- **Primary visual focal point:** rank #1 row — rendered with a gold highlight (`text-yellow-400` on rank number and username) to serve as the primary visual anchor for the screen
- **Empty state:** "No kills recorded yet. Be the first to land the killing blow."

### Title Shop (UI-03)

- **Layout:** list of 5 title cards, each showing: title label, cost in KB Currency, "Owned" badge if purchased, "Equip Title" / "Equipped" button state
- **Primary visual focal point:** KB balance display at the top of the TitleShop panel — rendered at 28px display size to anchor the screen and inform purchase decisions
- **Purchase flow:** single click → POST `/titles/:id/purchase` → success: button changes to "Equip Title" immediately (optimistic state update via store)
- **Equip flow:** single click → PATCH `/titles/:id/equip` → button shows "Equipped" (active state)
- **Insufficient KB Currency:** button disabled + `"Not enough KB"` label at 14px muted-foreground (same pattern as UpgradePanel insufficient gold)
- **No destructive actions in title shop** — purchases are permanent, equip is reversible

### Boss Lore (UI-04)

- **Location:** below boss name, above or alongside BossHpBar — inline, not an overlay
- **Copy length:** one sentence, max 80 characters (fits single line at 14px body)
- **Visibility:** always visible during fight; no toggle needed

### Player Title Badge (sidebar + leaderboard)

- **Rendering:** inline `<span>` after username, 14px, color `text-muted-foreground`, no background color
- **Example:** `Gorvax the Slayer [Annihilator]` where `[Annihilator]` is the badge span
- **Truncation:** username truncates before badge if space is constrained (`truncate` class on username span only)

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — Leaderboard nav | "Leaderboard" (noun label, nav link) |
| Primary CTA — Title Shop | "Buy Title" (verb + noun on purchase button) |
| Primary CTA — Equip title | "Equip Title" (verb + noun, button label) |
| Active equipped state | "Equipped" (past tense, disabled button label) |
| Killing blow announcement headline | "Killing Blow!" |
| Killing blow winner line | "{username} dealt the final blow" |
| Killing blow kill count | "Kill #{n}" |
| Post-fight screen heading | "Top Contributors" |
| Boss lore display | "{Boss Name}" (20px heading) + "{one-sentence lore}" (14px body) |
| Leaderboard heading | "Killing Blow Leaderboard" |
| Leaderboard column headers | "Rank", "Player", "Title", "Kills" |
| Leaderboard empty state heading | "No kills yet" |
| Leaderboard empty state body | "Be the first to land the killing blow." |
| Title shop heading | "Titles" |
| Title shop empty KB state | "Not enough KB" (inline, 14px, muted-foreground) |
| Title shop owned badge | "Owned" (14px, green-400 or muted, inline badge) |
| Title shop insufficient balance — upgrade nudge | "Earn KB by landing killing blows." (14px, muted) |
| Player sidebar — title display | "{username} · {title}" or "{username}" if no title |
| Error — purchase failed (server error) | "Purchase failed. Try again." (14px, destructive color) |
| Error — equip failed (server error) | "Could not equip title. Try again." (14px, destructive color) |
| Error — leaderboard load failed | "Could not load leaderboard. Refresh to try again." |
| KB Currency label | "KB" (abbreviation used throughout — no expansion needed) |

No destructive confirmation dialogs in Phase 4. Title purchases are irreversible but low-stakes (no confirmation prompt needed — cost is shown clearly before purchase).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | button, card, input, label, separator, progress (all already installed from prior phases) | not required |
| Third-party | none | not applicable — components.json registries: {} |

No new shadcn components need to be added in Phase 4. All Phase 4 UI is composed from:
- Existing shadcn primitives (Button, Card, CardHeader, CardContent)
- Custom components following OfflineRewardToast / PlayerSidebar patterns
- motion/react AnimatePresence for announcement overlay

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
