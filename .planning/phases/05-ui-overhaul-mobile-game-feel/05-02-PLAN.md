---
phase: 05-ui-overhaul-mobile-game-feel
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/BossHpBar.tsx
  - packages/client/src/components/BossSprite.tsx
  - packages/client/src/components/DamageNumbers.tsx
  - packages/client/src/components/KillingBlowAnnouncement.tsx
  - packages/client/src/components/PlayerSidebar.tsx
  - packages/client/src/components/UpgradePanel.tsx
  - packages/client/src/components/TitleShop.tsx
  - packages/client/src/components/Leaderboard.tsx
autonomous: true
requirements: [UX-02, UX-03, UX-04, UX-05, UX-08]

must_haves:
  truths:
    - "BossHpBar is 28px tall, rectangular (4px radius), with red glow and HP numbers above the bar"
    - "BossSprite renders 5 distinct SVG silhouettes keyed to bossNumber % 5 with idle breathe and death animations"
    - "DamageNumbers have random x-offset per hit, crit hits render at 28px yellow, normal at 20px white"
    - "KillingBlowAnnouncement uses spring entry animation with card glow and 28px winner name"
    - "All panels (PlayerSidebar, UpgradePanel, TitleShop, Leaderboard) use bg-black/40 backdrop-blur-sm dark glass surface"
    - "All font-semibold occurrences in touched files are replaced with font-normal (400) or font-bold (700)"
  artifacts:
    - path: "packages/client/src/components/BossHpBar.tsx"
      provides: "Dramatic HP bar with glow and pulse"
      contains: "hp-bar-glow"
    - path: "packages/client/src/components/BossSprite.tsx"
      provides: "SVG silhouette boss art with animations"
      contains: "SILHOUETTES"
    - path: "packages/client/src/components/DamageNumbers.tsx"
      provides: "Enhanced damage numbers with x-jitter and crit"
      contains: "xOffset"
    - path: "packages/client/src/components/KillingBlowAnnouncement.tsx"
      provides: "Spring-animated announcement with glow"
      contains: "spring"
    - path: "packages/client/src/components/PlayerSidebar.tsx"
      provides: "Dark glass panel surface"
      contains: "bg-black/40"
    - path: "packages/client/src/components/UpgradePanel.tsx"
      provides: "Dark glass panel surface"
      contains: "bg-black/40"
    - path: "packages/client/src/components/TitleShop.tsx"
      provides: "Dark glass panel surface"
      contains: "bg-black/40"
    - path: "packages/client/src/components/Leaderboard.tsx"
      provides: "Dark glass page surface"
      contains: "bg-black/40"
  key_links:
    - from: "packages/client/src/components/BossHpBar.tsx"
      to: "packages/client/src/index.css"
      via: "CSS variable --hp-bar-glow and class hp-bar-low"
      pattern: "hp-bar-glow|hp-bar-low"
    - from: "packages/client/src/components/BossSprite.tsx"
      to: "packages/client/src/index.css"
      via: "CSS class boss-hit-flash"
      pattern: "boss-hit-flash"
---

<objective>
Upgrade all existing components to Phase 5 visual spec: dramatic HP bar, SVG boss silhouettes, enhanced damage numbers, spring-animated KBA, and dark glass surface treatment on all panels.

Purpose: Each component is independently testable. These changes are visual-only — no layout or wiring changes. Components still render in the old Game.tsx layout until Plan 03 rewrites it.
Output: 8 component files upgraded to Phase 5 visual spec
</objective>

<execution_context>
@C:/Users/walbr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/walbr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-overhaul-mobile-game-feel/05-UI-SPEC.md
@.planning/phases/05-ui-overhaul-mobile-game-feel/05-RESEARCH.md

<interfaces>
<!-- Existing imports/types used by components that must be preserved -->

From packages/client/src/lib/socket.ts:
```typescript
export const socket = io(...)
// boss:damage_dealt is managed by DamageNumbers — do not remove (line 53 comment)
```

From @killing-blow/shared-types:
```typescript
export function formatNumber(n: Decimal): string
export { Decimal } // from break_eternity.js
export interface ActivePlayer { userId: string; username: string; damageDealt: number; equippedTitle: string | null }
```

From stores:
```typescript
// announcementStore
interface AnnouncementState { active: boolean; winnerUsername: string; winnerTitle: string | null; winnerKillCount: number; topContributors: { username: string; title: string | null; damageDealt: number }[] }

// progressionStore
interface ProgressionState { goldBalance: string; atkLevel: number; critLevel: number; spdLevel: number; isTabActive: boolean }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade BossHpBar, BossSprite, and DamageNumbers to Phase 5 visual spec</name>
  <files>
    packages/client/src/components/BossHpBar.tsx
    packages/client/src/components/BossSprite.tsx
    packages/client/src/components/DamageNumbers.tsx
  </files>
  <read_first>
    - packages/client/src/components/BossHpBar.tsx
    - packages/client/src/components/BossSprite.tsx
    - packages/client/src/components/DamageNumbers.tsx
    - packages/client/src/lib/socket.ts
    - .planning/phases/05-ui-overhaul-mobile-game-feel/05-UI-SPEC.md
    - .planning/phases/05-ui-overhaul-mobile-game-feel/05-RESEARCH.md
  </read_first>
  <action>
**BossHpBar.tsx** — Rewrite to Phase 5 HP Bar Contract. Preserve the existing interface `BossHpBarProps { hp: number; maxHp: number }` and the existing imports from `@killing-blow/shared-types`.

Changes:
1. Calculate `const isLowHp = percent < 20`
2. Move HP numbers ABOVE the bar (currently below). Use `text-[28px] font-bold leading-[1.1]` (replace font-semibold with font-bold)
3. Progress bar container: height `28px` (was 16px), borderRadius `4px` (was 9999px pill), backgroundColor `#0f0f0f` (was #27272a), border `1px solid rgba(220,38,38,0.2)`, overflow `visible` (was hidden — allow glow bleed)
4. Progress bar fill: backgroundColor `#dc2626`, borderRadius `4px`, transition `width 200ms ease-out` (was 150ms linear), boxShadow `var(--hp-bar-glow)`
5. Apply className `hp-bar-low` to the fill div when `isLowHp` is true (CSS pulse animation from index.css)
6. Keep all aria attributes: role="progressbar", aria-label="Boss HP", aria-valuenow, aria-valuemin, aria-valuemax

Exact render structure from RESEARCH.md "BossHpBar Phase 5 Contract" code example.

**BossSprite.tsx** — Full rewrite to SVG silhouettes with animations. Preserve existing interface `BossSpriteProps { bossNumber: number; isDefeated: boolean }`.

Changes:
1. Add import: `import { useReducedMotion } from 'motion/react'`
2. Define `const SILHOUETTES` array with 5 SVG path elements (dragon, golem, wraith, spider, lich). Each is a simple dark silhouette shape using basic SVG `<path>` or `<polygon>` elements within a `viewBox="0 0 200 200"`. These are simple recognizable outlines — not detailed art. Use filled black paths that look correct as silhouettes against the red glow filter.
3. Select silhouette: `const silhouette = SILHOUETTES[bossNumber % 5]`
4. Outer container: `relative flex items-center justify-center` with min-height 200px mobile, 280px desktop (`min-h-[200px] lg:min-h-[280px]`)
5. Idle breathe animation: wrap SVG in a `motion.div` with `animate={{ scale: [1, 1.02, 1] }}` and `transition={{ duration: 3, ease: 'easeInOut', repeat: Infinity }}`. Skip if `prefersReducedMotion`.
6. Death animation: conditional `animate` prop — when `isDefeated`: `{ scale: 1.4, opacity: 0, filter: 'drop-shadow(0 0 24px rgba(220,38,38,0.9))' }`, else `{ scale: 1, opacity: 1 }`
7. SVG element: `<svg viewBox="0 0 200 200" className="w-[200px] h-[200px] lg:w-[280px] lg:h-[280px]" style={{ filter: 'drop-shadow(0 0 16px rgba(220,38,38,0.5))' }}>`
8. Keep `aria-hidden="true"` on outer container
9. Keep `AnimatePresence mode="wait"` with `key={bossNumber}` for boss transitions
10. Hit flash: add a `useState` for hit flash, listen to `boss:damage_dealt` socket event. On event, add `boss-hit-flash` CSS class to the SVG wrapper for 80ms then remove it. Import `socket` from `../lib/socket.js` and `useState, useEffect` from react.

IMPORTANT: The hit flash socket listener is INDEPENDENT of DamageNumbers' listener. Socket.IO allows multiple listeners per event. Do NOT touch DamageNumbers' existing listener.

**DamageNumbers.tsx** — Enhance with x-jitter, crit differentiation, and pop-in animation. Preserve the socket listener pattern (independent `boss:damage_dealt` subscription — do NOT move to subscribeToGame).

Changes:
1. Extend Hit interface: add `xOffset: number` field
2. On socket event handler, generate `const xOffset = (Math.random() - 0.5) * 40` (range -20 to +20px) and include in hit object
3. Determine crit: `const isCrit = amount >= 75` (3x BASE_DAMAGE=25)
4. Remove `overflow-hidden` from the container div (was clipping float animation). Container: `className="absolute inset-0 pointer-events-none"` (no overflow-hidden)
5. Each hit div: use `style={{ left: \`calc(50% + ${hit.xOffset}px)\` }}` instead of `left-1/2 -translate-x-1/2`
6. Crit styling: `text-[28px] text-yellow-400` with `textShadow: '0 0 8px rgba(245,158,11,0.8)'`. Normal: `text-[20px] text-white`
7. All damage numbers: `font-bold` (replace font-semibold)
8. Updated animation — initial: `{ opacity: 0, scale: 1.2, y: 0 }`, animate: `{ opacity: [0, 1, 1, 0], scale: [1.2, 1.0, 0.8], y: -80 }`, transition: `{ duration: 1.2, times: [0, 0.1, 0.7, 1], ease: 'easeOut' }`
9. Keep cleanup timeout at 1200ms to match animation duration
  </action>
  <verify>
    <automated>grep -c "hp-bar-glow" packages/client/src/components/BossHpBar.tsx && grep -c "SILHOUETTES" packages/client/src/components/BossSprite.tsx && grep -c "xOffset" packages/client/src/components/DamageNumbers.tsx && grep -c "font-semibold" packages/client/src/components/BossHpBar.tsx packages/client/src/components/BossSprite.tsx packages/client/src/components/DamageNumbers.tsx</automated>
  </verify>
  <acceptance_criteria>
    - BossHpBar.tsx contains `height: '28px'` (not 16px)
    - BossHpBar.tsx contains `borderRadius: '4px'` (not 9999px)
    - BossHpBar.tsx contains `var(--hp-bar-glow)` in boxShadow
    - BossHpBar.tsx contains `hp-bar-low` class conditionally applied
    - BossHpBar.tsx contains `overflow: 'visible'` (not hidden)
    - BossHpBar.tsx contains `font-bold` (not font-semibold)
    - BossHpBar.tsx HP numbers paragraph appears BEFORE the progressbar div
    - BossSprite.tsx contains `const SILHOUETTES` array with 5 elements
    - BossSprite.tsx contains `bossNumber % 5` for silhouette selection
    - BossSprite.tsx contains `useReducedMotion` import from `motion/react`
    - BossSprite.tsx contains `scale: [1, 1.02, 1]` for idle breathe
    - BossSprite.tsx contains `scale: 1.4` in death animation
    - BossSprite.tsx contains `drop-shadow(0 0 16px rgba(220,38,38,0.5))` filter
    - BossSprite.tsx contains `boss-hit-flash` class for hit feedback
    - BossSprite.tsx contains `viewBox="0 0 200 200"`
    - DamageNumbers.tsx contains `xOffset` property in Hit interface
    - DamageNumbers.tsx contains `(Math.random() - 0.5) * 40` for offset calculation
    - DamageNumbers.tsx contains `amount >= 75` for crit detection
    - DamageNumbers.tsx contains `text-[28px] text-yellow-400` for crit styling
    - DamageNumbers.tsx contains `text-[20px] text-white` for normal styling
    - DamageNumbers.tsx container does NOT contain `overflow-hidden`
    - DamageNumbers.tsx contains `font-bold` (not font-semibold)
    - DamageNumbers.tsx contains `times: [0, 0.1, 0.7, 1]` for keyframe timing
    - No file in this task contains `font-semibold`
  </acceptance_criteria>
  <done>BossHpBar renders 28px tall bar with glow and pulse. BossSprite renders 5 SVG silhouettes with breathe/hit/death animations. DamageNumbers use x-jitter, crit sizing, and pop-in animation.</done>
</task>

<task type="auto">
  <name>Task 2: Apply dark glass surface to KillingBlowAnnouncement, PlayerSidebar, UpgradePanel, TitleShop, and Leaderboard</name>
  <files>
    packages/client/src/components/KillingBlowAnnouncement.tsx
    packages/client/src/components/PlayerSidebar.tsx
    packages/client/src/components/UpgradePanel.tsx
    packages/client/src/components/TitleShop.tsx
    packages/client/src/components/Leaderboard.tsx
  </files>
  <read_first>
    - packages/client/src/components/KillingBlowAnnouncement.tsx
    - packages/client/src/components/PlayerSidebar.tsx
    - packages/client/src/components/UpgradePanel.tsx
    - packages/client/src/components/TitleShop.tsx
    - packages/client/src/components/Leaderboard.tsx
    - .planning/phases/05-ui-overhaul-mobile-game-feel/05-UI-SPEC.md
  </read_first>
  <action>
**KillingBlowAnnouncement.tsx** — Enhance animation and visual treatment:

1. Replace transition from `{ duration: 0.2 }` to `{ type: 'spring', stiffness: 400, damping: 20 }`
2. Replace initial/animate/exit: initial `{ opacity: 0, scale: 0.5 }`, animate `{ opacity: 1, scale: 1 }`, exit `{ opacity: 0, scale: 0.5 }`
3. Backdrop: change `bg-black/70` to `bg-black/80`
4. Content card: replace `bg-card border border-border` with `bg-black/80 border border-white/10`. Add inline style `boxShadow: '0 0 40px rgba(220,38,38,0.4)'`
5. "Killing Blow!" headline: change `font-semibold` to `font-bold`
6. Winner name paragraph: change `text-[20px] font-semibold` to `text-[28px] font-bold`
7. Kill count: change `font-semibold` to `font-bold`
8. "Top Contributors" heading: change `font-semibold` to `font-bold`
9. Contributor damage amount: change `font-semibold` to `font-bold`
10. All other data/layout stays identical — do not change any store imports, data display, or conditional logic

**PlayerSidebar.tsx** — Dark glass surface:

1. Outer container: keep `w-64 flex-shrink-0` (will be overridden by Game.tsx layout in Plan 03)
2. Inner panel: replace `bg-card border border-border rounded-lg p-4` with `bg-black/40 backdrop-blur-sm border border-white/10 rounded-lg p-4` and add `style={{ boxShadow: 'var(--panel-border-glow)' }}`
3. Heading "In the Fight": change `font-semibold` to `font-bold`, change `text-sm` to `text-[16px]`
4. Player damage: change `font-semibold` to `font-bold`
5. Add `overflow-y-auto max-h-[300px]` to the `<ul>` element for scroll when >8 players

**UpgradePanel.tsx** — Dark glass surface:

1. Replace `<Card className="flex-shrink-0 w-64">` with a plain `<div className="flex-shrink-0 w-64 bg-black/40 backdrop-blur-sm border border-white/10 rounded-lg" style={{ boxShadow: 'var(--panel-border-glow)' }}>`
2. Replace `<CardHeader className="pb-2">` with `<div className="p-4 pb-2">`
3. Replace `<CardContent className="space-y-4">` with `<div className="px-4 pb-4 space-y-4">`
4. Remove Card, CardHeader, CardContent imports (no longer used)
5. Heading "Upgrades": change `font-semibold` to `font-bold`
6. Stat labels (ATK, CRIT, SPD): change `font-semibold` to `font-bold`
7. "Attack to earn gold" heading: change `font-semibold` to `font-bold`

**TitleShop.tsx** — Dark glass surface:

1. Outer `<div className="w-72 flex-shrink-0">` stays
2. Inner panel: replace `bg-card border border-border rounded-lg p-4` with `bg-black/40 backdrop-blur-sm border border-white/10 rounded-lg p-4` and add `style={{ boxShadow: 'var(--panel-border-glow)' }}`
3. Heading "Titles": change `font-semibold` to `font-bold`

**Leaderboard.tsx** — Dark glass page treatment:

1. Root div: keep `min-h-screen p-8 max-w-2xl mx-auto`
2. Heading: change `font-semibold` to `font-bold`
3. Wrap the leaderboard table in a dark glass panel: add `<div className="bg-black/40 backdrop-blur-sm border border-white/10 rounded-lg p-4" style={{ boxShadow: 'var(--panel-border-glow)' }}>` around the column headers, rows, and pagination
4. Column headers: change `font-semibold` to `font-bold`
5. Rank #1 yellow text: change `font-semibold` to `font-bold`
6. Kill count: change `font-semibold` to `font-bold`
7. "No kills yet" heading: change `font-semibold` to `font-bold`
8. Border rows: change `border-border` to `border-white/10`
  </action>
  <verify>
    <automated>grep -c "bg-black/40" packages/client/src/components/PlayerSidebar.tsx packages/client/src/components/UpgradePanel.tsx packages/client/src/components/TitleShop.tsx packages/client/src/components/Leaderboard.tsx packages/client/src/components/KillingBlowAnnouncement.tsx && grep -rc "font-semibold" packages/client/src/components/KillingBlowAnnouncement.tsx packages/client/src/components/PlayerSidebar.tsx packages/client/src/components/UpgradePanel.tsx packages/client/src/components/TitleShop.tsx packages/client/src/components/Leaderboard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - KillingBlowAnnouncement.tsx contains `type: 'spring'` in transition
    - KillingBlowAnnouncement.tsx contains `stiffness: 400`
    - KillingBlowAnnouncement.tsx contains `scale: 0.5` in initial
    - KillingBlowAnnouncement.tsx contains `bg-black/80` for both backdrop and card
    - KillingBlowAnnouncement.tsx contains `border-white/10`
    - KillingBlowAnnouncement.tsx contains `boxShadow: '0 0 40px rgba(220,38,38,0.4)'`
    - KillingBlowAnnouncement.tsx winner name is `text-[28px] font-bold` (not text-[20px] font-semibold)
    - PlayerSidebar.tsx contains `bg-black/40 backdrop-blur-sm border border-white/10`
    - PlayerSidebar.tsx contains `var(--panel-border-glow)`
    - PlayerSidebar.tsx contains `overflow-y-auto`
    - UpgradePanel.tsx contains `bg-black/40 backdrop-blur-sm`
    - UpgradePanel.tsx does NOT import Card, CardHeader, or CardContent
    - UpgradePanel.tsx contains `var(--panel-border-glow)`
    - TitleShop.tsx contains `bg-black/40 backdrop-blur-sm border border-white/10`
    - TitleShop.tsx contains `var(--panel-border-glow)`
    - Leaderboard.tsx contains `bg-black/40 backdrop-blur-sm`
    - Leaderboard.tsx contains `border-white/10` on row borders
    - NONE of these 5 files contain `font-semibold` — all replaced with `font-bold` or `font-normal`
  </acceptance_criteria>
  <done>All panels use dark glass surface (bg-black/40 backdrop-blur-sm border-white/10). KBA has spring animation and glow. All font-semibold replaced with font-bold across touched components.</done>
</task>

</tasks>

<verification>
1. `grep -rc "font-semibold" packages/client/src/components/BossHpBar.tsx packages/client/src/components/BossSprite.tsx packages/client/src/components/DamageNumbers.tsx packages/client/src/components/KillingBlowAnnouncement.tsx packages/client/src/components/PlayerSidebar.tsx packages/client/src/components/UpgradePanel.tsx packages/client/src/components/TitleShop.tsx packages/client/src/components/Leaderboard.tsx` — all return 0
2. `grep -c "bg-black/40" packages/client/src/components/PlayerSidebar.tsx` — returns 1+
3. `grep -c "SILHOUETTES" packages/client/src/components/BossSprite.tsx` — returns 1+
4. `pnpm --filter @killing-blow/client test --run` passes
</verification>

<success_criteria>
- BossHpBar: 28px, rectangular, glow, pulse on low HP, numbers above bar
- BossSprite: 5 SVG silhouettes, idle breathe, hit flash, death animation
- DamageNumbers: x-jitter, crit at 28px yellow, normal at 20px white, pop-in animation
- KBA: spring entry, glow card, 28px winner name
- All panels: dark glass surface treatment
- Zero font-semibold remaining in any touched file
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-overhaul-mobile-game-feel/05-02-SUMMARY.md`
</output>
